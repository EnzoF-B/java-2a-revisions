# java-2a-revisions
Les programmes java qu'on a pu faire en 1A pour vos révisions

Chaque dossier est un "projet" et contient un dossier src où y'a le code.

animation : l'idée était de faire une stack qui contient des figures avec les dimensions qu'on insère dans une chaîne de caractères

calc : pas besoin de l'expliquer, une calculatrice tout simplement (je crois que le fichier n'est pas complet j'avais pas entièrement réussi le td)

geom : faire une fenetre où on peut dessiner les figures qu'on veut

table : reprendre l'idée d'une base de données SQL sous forme d'un tableau. on insère les données directement dans le fichier code
